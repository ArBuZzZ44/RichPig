<% provide :page_title, 'Not completed capitals' %>
<% currently_at('Not completed') %>

<div class="container text-center">
	<div class="row justify-content-between">
		<div class="col-auto me-auto">
			<h1>Here are all your current goals</h1>
		</div>
		<div class="col-auto">
			<%= link_to "New goal", new_capital_path, class: 'btn btn-primary btn-lg' %>
		</div>
	</div>
</div>

<%= pagination(@pagy) %>

<%= render partial: 'capitals/capital', collection: @capitals, as: :capital %>

<%= pagination(@pagy) %>