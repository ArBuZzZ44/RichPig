<% provide :page_title, 'Completed capitals' %>
<% currently_at('Completed') %>

<div class="container text-center">
	<div class="row justify-content-between">
		<div class="col-auto me-auto">
			<h1>Your achieved goals:</h1>
		</div>
		<div class="col-auto">
			<%= link_to "New goal", new_capital_path, class: 'btn btn-primary btn-lg' %>
		</div>
	</div>
</div>

<%= pagination(@pagy) %>
<%= render partial: 'capitals/capital', collection: @capitals, as: :capital %>
<%= pagination(@pagy) %>